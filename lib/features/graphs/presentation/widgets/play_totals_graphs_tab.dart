import 'package:flutter/material.dart';

class PlayTotalsGraphsTab extends StatelessWidget {
  const PlayTotalsGraphsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('Play Totals Tab');
  }
}
