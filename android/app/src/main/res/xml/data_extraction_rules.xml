<?xml version="1.0" encoding="utf-8"?>
<!--
  Everything is excluded from both transfer modes. The servers table holds each
  server's Tautulli API key alongside its internal and external addresses and any
  custom headers, so a restore would hand a working credential set to whatever
  device the backup lands on.

  allowBackup="false" covers this on its own up to API 30, but from Android 12 it
  only disables cloud backup and leaves device-to-device transfer running, so the
  rules below are what actually close that path. path="." means the whole domain.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
        <exclude domain="device_root" path="." />
        <exclude domain="device_file" path="." />
        <exclude domain="device_database" path="." />
        <exclude domain="device_sharedpref" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
        <exclude domain="device_root" path="." />
        <exclude domain="device_file" path="." />
        <exclude domain="device_database" path="." />
        <exclude domain="device_sharedpref" path="." />
    </device-transfer>
</data-extraction-rules>
